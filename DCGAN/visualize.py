import pickle
import os
import matplotlib.pyplot as plt

"""file_path = './DCGAN/checkpoint\losses\cifar10.pkl'

# Open the file in binary read mode
with open(file_path, 'rb') as file:
    # Load the dictionary from the file
    my_dict = pickle.load(file)

import matplotlib.pyplot as plt
import numpy as np 
fig, ax1 = plt.subplots()

# Plot for the first y-axis
ax1.plot(my_dict["loss_G_train"], color='b', label="loss_G_train")
ax1.set_ylabel("loss_G_train", color='b')
ax1.tick_params('y', colors='b')

# Create a twin Axes sharing the xaxis
ax2 = ax1.twinx()
ax2.plot(my_dict["loss_D_train"], color='r', label="loss_D_train")

ax2.set_ylabel("loss_D_train", color='r')
ax2.tick_params('y', colors='r')

plt.show()"""


    